---
const { id, title, headingLevel = "h2" } = Astro.props;
const Heading = headingLevel;
---

<section id={id}>
    <Heading>{title}</Heading>
    <slot />
</section>

<style>
    section {
        margin-top: 5rem;
        padding: 0 20px;
    }

    h2 {
        font-size: 1.8rem;
        margin-bottom: 16px;
        border-bottom: 2px solid #ccc;
    }

    ul {
        list-style-type: disc;
        padding-left: 20px;
    }

    li {
        margin-bottom: 8px;
        font-size: 1rem;
        color: #333;
    }
</style>

<style is:inline>
    p {
        font-size: 1.2rem;
        line-height: 1.6;
        color: #333;
    }
</style>
